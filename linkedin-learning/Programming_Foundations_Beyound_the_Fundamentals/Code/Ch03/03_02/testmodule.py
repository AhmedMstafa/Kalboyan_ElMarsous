# Create my module

# import the module:
import testmodule

# A function that prints the multiplication of two numbers


def mult(x, y):
    print(f'{x} * {y} = {x * y}')

# f'{}' called formatting string 
# When using f-Strings to display variables,
# you only need to specify the names of the variables inside a set of curly braces {}.
# And at runtime, all variable names will be replaced with their respective values.
